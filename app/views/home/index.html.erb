<div class="container">
  <a class="btn btn-default" href="/post/new" role="button">게시글 쓰러가기</a>
</div>
<div class="container">
  <% @posts.each do |post| %>
      <h2><%= post.title %></h2>
      <p><%= post.content %></p>
      <img src="<%= post.img_url %>">
  <% end %>
</div>